// Root build: shared settings for all subprojects. Groovy DSL on purpose.
allprojects {
    group = rootProject.group
    version = rootProject.version
    repositories {
        mavenCentral()
    }
}
